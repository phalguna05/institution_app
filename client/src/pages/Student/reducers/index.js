const initialState = { dashboardDisplay: 0 };

const studentReducer = (state = initialState, action) => {
  const { type, data } = action;
  switch (type) {
    default:
      return state;
  }
};
export { studentReducer };
